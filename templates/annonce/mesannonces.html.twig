    {% extends 'base.html.twig' %}

    {% block title %}Mes annonces{% endblock %}

    {% block body %}

{% if app.user %}
    
    <div class="container">
        <div class="row">
            {% for annonce in annonces %}
            {% if annonce.iduser == app.user %}
            <div class="col-sm-6 col-4">
                <div class="card" style="width: 18rem;">
                    {# <img src="{{  img.annonce.id }}" class="card-img-top" alt="..."> #}
                    {# <img src="{{  annonce.id|img }}" class="card-img-top" alt="..."> #}

    {% set count = 0 %}
    {% for imag in imgs %}                
        {% set id_annn = imag.idAnnonce|replace({'   ': ""}) %}
        {% set annoce_id = annonce.id %}
        {% if count == 0 %}
            {% if id_annn == annoce_id %}
                <img src="{{  imag.lien }}" class="card-img-top" alt="...">
                {% set count = 1 %}
            {% endif %} 
        {% endif %} 
    {% endfor %}
        {% if count == 0 %}
                <img src="\img_website\home-outline.svg" class="card-img-top" height="114" alt="...">
        {% endif %} 
                    <div class="card-body">
                        <h5 class="card-title">{{ annonce.titre }}</h5>
                        <p class="card-text">
                        <div class="truncate">
                        {{ annonce.description  }}
                        </div>
                        {# <br>
                        {{ annonce.adresse }}
                        <br> #}
                        {{ annonce.ville }}
                        <br>
                        <div class="">
                        {{ annonce.prix }} € / par nuit
                        </div>
                        </p>
                        <a href="{{ path('app_annonce_show', {'id': annonce.id}) }}" class="btn btn-primary">Voir Plus</a>
                    </div>
                </div>
            </div>
                {% endif %}
            {% endfor %}
    </div>
        </div>
{% endif %}
    {% endblock %}
