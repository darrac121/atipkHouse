{% extends 'base.html.twig' %}

{% block title %}DocumentProprietaire{% endblock %}

{% block body %}
    <h1>DocumentProprietaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_proprietaire.id }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ document_proprietaire.lien }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ document_proprietaire.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_document_proprietaire_index') }}">back to list</a>

    <a href="{{ path('app_document_proprietaire_edit', {'id': document_proprietaire.id}) }}">edit</a>

    {{ include('document_proprietaire/_delete_form.html.twig') }}
{% endblock %}
