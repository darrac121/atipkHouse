<form method="post" action="{{ path('app_document_proprietaire_delete', {'id': document_proprietaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_proprietaire.id) }}">
    <button class="btn">Delete</button>
</form>
