<form method="post" action="{{ path('app_image_annonce_delete', {'id': image_annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_annonce.id) }}">
    <button class="btn">Delete</button>
</form>
