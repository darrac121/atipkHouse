{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
<div class="container">
    <h1>Categorie</h1>
    <a class="btn btn-success" href="{{ path('app_category_new') }}">Cree un nouveaux</a>
    <table class="table">
        <thead>
            <tr>
                <th>Nom de la categorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                {# <td>{{ category.id }}</td> #}
                <td>{{ category.libelle }}</td>
                {# <td>{{ category.status }}</td> #}
                <td>
                    <a class="btn btn-success" href="{{ path('app_category_show', {'id': category.id}) }}">Voir</a>
                    <a class="btn btn-info" href="{{ path('app_category_edit', {'id': category.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Pas de categorie</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
