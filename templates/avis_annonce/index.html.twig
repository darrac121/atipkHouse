{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>
    <table class="table">
        <thead>
            <tr>
                <th>Message</th>
                <th>Rating</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avis_annonce in avis_annonces %}
            <tr>
                <td>{{ avis_annonce.message }}</td>
                <td>{{ avis_annonce.rating }}</td>
                <td>
                    <a href="{{ path('app_avis_annonce_show', {'id': avis_annonce.id}) }}">show</a>
                    <a href="{{ path('app_avis_annonce_edit', {'id': avis_annonce.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Il y a pas d'avis</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('app_avis_annonce_new') }}">Create new</a> #}
{% endblock %}
