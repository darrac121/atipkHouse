<form method="post" action="{{ path('app_avis_annonce_delete', {'id': avis_annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avis_annonce.id) }}">
    <button class="btn">Delete</button>
</form>
