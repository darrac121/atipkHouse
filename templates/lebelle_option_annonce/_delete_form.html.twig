<form method="post" action="{{ path('app_lebelle_option_annonce_delete', {'id': lebelle_option_annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lebelle_option_annonce.id) }}">
    <button class="btn">Delete</button>
</form>
