{% extends 'base.html.twig' %}

{% block title %}Champs personnalisé{% endblock %}

{% block body %}
    <h1>Champs personnalisé</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Value</th>
                <td>{{ lebelle_option_annonce.value }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ lebelle_option_annonce.status }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-dark" href="{{ path('app_lebelle_option_annonce_index') }}">back to list</a>

    <a class="btn btn-info" href="{{ path('app_lebelle_option_annonce_edit', {'id': lebelle_option_annonce.id}) }}">Modifier</a>

    {{ include('lebelle_option_annonce/_delete_form.html.twig') }}
{% endblock %}
