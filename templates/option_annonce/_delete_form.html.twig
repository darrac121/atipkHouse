<form method="post" action="{{ path('app_option_annonce_delete', {'id': option_annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option_annonce.id) }}">
    <button class="btn">Delete</button>
</form>
