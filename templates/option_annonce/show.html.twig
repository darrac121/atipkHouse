{% extends 'base.html.twig' %}

{% block title %}OptionAnnonce{% endblock %}

{% block body %}
    <h1>OptionAnnonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option_annonce.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ option_annonce.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_option_annonce_index') }}">back to list</a>

    <a href="{{ path('app_option_annonce_edit', {'id': option_annonce.id}) }}">edit</a>

    {{ include('option_annonce/_delete_form.html.twig') }}
{% endblock %}
